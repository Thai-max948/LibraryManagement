<UserControl x:Class="LibraryManagement.Views.BorrowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LibraryManagement.ViewModels">

    <UserControl.DataContext>
        <vm:BorrowViewModel/>
    </UserControl.DataContext>

    <StackPanel Margin="10">
        <TextBlock Text="📖 Borrow Book" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>

        <TextBlock Text="Reader" Foreground="{StaticResource TextBrush}"/>
        <ComboBox ItemsSource="{Binding Readers}" SelectedItem="{Binding SelectedReader}"
                  DisplayMemberPath="FullName" Margin="0,4,0,10" Padding="6"/>

        <TextBlock Text="Book" Foreground="{StaticResource TextBrush}"/>
        <ComboBox ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}"
                  DisplayMemberPath="Title" Margin="0,4,0,10" Padding="6"/>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Borrow Date" Foreground="{StaticResource TextBrush}"/>
                <DatePicker SelectedDate="{Binding BorrowDate}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Due Date" Foreground="{StaticResource TextBrush}"/>
                <DatePicker SelectedDate="{Binding DueDate}" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <Button Content="📖 Borrow Book" Command="{Binding BorrowCommand}"
                Style="{StaticResource PrimaryButtonStyle}"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                HorizontalAlignment="Right" Margin="0,0,0,16"/>

        <TextBlock Text="Current Borrowings" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
        <DataGrid ItemsSource="{Binding CurrentBorrowings}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{StaticResource AppDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Reader" Binding="{Binding ReaderName}" Width="*"/>
                <DataGridTextColumn Header="Book" Binding="{Binding BookTitle}" Width="*"/>
                <DataGridTextColumn Header="Borrow Date" Binding="{Binding BorrowDate}" Width="120"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>